{
  "manifest_version": 3,
  "name": "Seerr Search Filter",
  "version": "0.1.0",
  "description": "Adds a client-side filter panel (type, genre, year, rating, language, availability, actor) to Seerr's search results page.",
  "permissions": ["storage", "scripting"],
  "optional_host_permissions": ["*://*/*"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "options_page": "options/options.html",
  "action": {
    "default_title": "Seerr Search Filter — click to configure your instance URL"
  }
}
